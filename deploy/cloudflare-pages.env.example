# Cloudflare Pages → Project → Settings → Environment variables (Production + Preview)

VITE_API_URL=https://omnisecure-api.fly.dev

# Build settings (Dashboard → Builds)
# Framework preset: None
# Root directory: /
# Build command: pnpm install && pnpm build:vault
# Build output directory: apps/vault-web/dist
# Node version: 20
